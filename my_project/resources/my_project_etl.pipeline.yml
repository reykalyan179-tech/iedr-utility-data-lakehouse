# The main pipeline for my_project

resources:
  pipelines:
    my_project_etl:
      name: my_project_etl_${bundle.target}
      catalog: ${var.catalog}
      schema: ${var.target_schema}
      serverless: true
      development: true

      libraries:
        - notebook:
            path: notebooks/silver_transformation.ipynb



