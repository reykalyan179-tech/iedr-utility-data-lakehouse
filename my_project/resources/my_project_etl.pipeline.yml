resources:
  pipelines:
    my_project_etl:
      name: iedr-${bundle.target}-pipeline

      catalog: ${var.catalog}
      schema: ${var.target_schema}

      serverless: true
      development: ${var.dlt_development}

      libraries:
        - notebook:
            path: /Workspace/Users/mravindra0911@gmail.com/notebooks/silver_transformation




